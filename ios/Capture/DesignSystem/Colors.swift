import SwiftUI

extension Color {
    /// Primary brand blue — sampled from the "Book Now" CTA and the voucher wedding card.
    static let captureBlue = Color(red: 0.43, green: 0.55, blue: 0.99)
    /// Accent pink — sampled from the portrait voucher card.
    static let capturePink = Color(red: 0.96, green: 0.40, blue: 0.56)
    /// Soft canvas the app sits on.
    static let captureBackground = Color(red: 0.97, green: 0.97, blue: 0.99)
    /// Ink for primary text.
    static let captureInk = Color(red: 0.09, green: 0.13, blue: 0.25)
    /// Muted grey for secondary text.
    static let captureInkMuted = Color(red: 0.44, green: 0.48, blue: 0.56)
    /// Price chip background from the Vallentyne booking screen.
    static let capturePriceChip = Color(red: 1.0, green: 0.88, blue: 0.71)
    /// Price chip text.
    static let capturePriceInk = Color(red: 0.94, green: 0.54, blue: 0.18)
}
